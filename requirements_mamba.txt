# Unified environment requirements for IS-Fusion + PointMamba (Mamba encoder)
#
# Base environment: CUDA 11.8+, PyTorch 2.0+
#
# Install order:
#   1. Install PyTorch first (example for CUDA 11.8):
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   2. pip install -r requirements_mamba.txt
#   3. Build CUDA extensions:
#      pip install -e /path/to/PointMamba/mamba/   (mamba_ssm)
#      cd IS-Fusion && pip install -v -e .
#
# ---- Shared / Aligned Dependencies ----
numpy>=1.22.0,<2.0.0
scipy>=1.7.0

# ---- PointMamba / Mamba SSM Dependencies ----
einops==0.7.0
timm==0.4.5
addict==2.4.0
transformers==4.36.2        # PINNED: mamba_ssm imports GreedySearchDecoderOnlyOutput (removed in 5.x)
triton==2.2.0
causal-conv1d==1.1.1        # Optional but recommended for Mamba performance
packaging
ninja

# ---- IS-Fusion / MMDetection3D Dependencies ----
mmengine>=0.7.0,<1.0.0
mmcv>=2.0.0,<2.2.0            # Build from source with matching CUDA/PyTorch
mmdet>=3.0.0,<3.4.0           # Build from source
mmsegmentation>=1.0.0,<1.3.0  # Build from source
numba>=0.57.0
networkx>=2.2,<2.3
tensorboard
nuscenes-devkit
lyft_dataset_sdk
trimesh>=2.35.39
scikit-image
plyfile

# ---- PointMamba CUDA Extensions (install separately) ----
# knn-cuda==0.2              # pip install from PointMamba/extensions or PyPI
# pointnet2-ops==3.0.0       # No longer needed (pillarization replaces FPS+KNN)
